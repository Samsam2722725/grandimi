name: Deploy Grandimi

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy Backend to Railway
        run: |
          echo "🚀 Deploying Backend..."
          # Simulate deployment (Railway auto-deploys via GitHub integration)
          echo "✅ Railway receives the push and auto-deploys"

      - name: Verify Deployment
        run: |
          echo "✅ All systems deployed!"
          echo "Frontend: https://frontend-57k8xlnul-samuelgarbil-3776s-projects.vercel.app"
          echo "Backend: Check Railway dashboard for URL"

